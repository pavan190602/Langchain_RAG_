events {
    worker_connections 1024;
}

http {
    # When external access is disabled, nginx returns 503
    server {
        listen 80 default_server;
        server_name _;

        location / {
            return 503 "Service temporarily unavailable. External access is disabled.\n";
            add_header Content-Type text/plain;
        }
    }
}
